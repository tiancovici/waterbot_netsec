'use strict'

var start = Date.now()
var iterations = 1000000
var hack = null

for (var n = 0; n < iterations; n++) {
  var err = {}
  Error.captureStackTrace(err)
  hack = err.foo
}

var diff = Date.now() - start

console.log('%d ms per iteration (%s)', diff / iterations, hack)
